# Pendências reais — Ecossistema Escolar

> Lista **só do que falta**, sem o histórico narrativo/justificativas — isso continua em `RELATORIO_BAUA_CODIGO.md`. Este arquivo é o que deve ser consultado pra saber "o que falta fazer" e mantido atualizado daqui pra frente: quando um item for implementado, **apague a linha daqui** (não deixe virar um segundo checklist desatualizado).
>
> **Rodada de fechamento em 2026-08-19**: implementados nesta sessão — trocar capa/cor da turma pelo Representante (UI + bug real corrigido na checagem de permissão, ver nota abaixo), limite máximo de membros em grupo de tarefa (RF03/RF04 completo: convite/solicitação/aceite, incluindo o gap não documentado de excluir o grupo solo original do aluno ao entrar em outro), endpoint de alunos disponíveis pra convite (`ConviteGrupoModal.tsx` não usa mais dados mockados), rate limiting em upload (`/api/upload/*`, `/api/anexo`) e rotas de admin de plataforma/sugestão. Também confirmado nesta rodada: a Recomendação de Estudo por IA (tela de consumo pelo aluno) e o canal WhatsApp **já estavam implementados** de sessões anteriores — a lista abaixo estava desatualizada nesses dois pontos, não o código. Detalhes de cada correção/achado real em `docs/SPEC_PENDENCIAS_PRODUTO_2026-08-19.md`, junto com o que ainda fica de fato pendente (seed/bootstrap de banco, testes, copy da landing page) — spec nova porque são decisões que precisam de mais contexto que uma linha de checklist.
>
> **Achado real durante a implementação do limite de membros**: `backend/services/turma.service.ts#validarPermissaoCapaTurma` chamava `ConversaGrupoService.getFuncaoNaTurma` passando CPF, mas esse método já espera `UsuarioGUID` desde a migração CPF→GUID — a checagem de permissão do Representante/Vice-Representante sempre falhava silenciosamente (`tsc` não pega comparação `string`/`string`), então mesmo com a UI pronta, um Representante de verdade nunca conseguiria trocar a capa da turma (só Coordenação/Direção, via `validarPermissaoEscrita`, conseguia). Corrigido — resíduo da migração de sessões anteriores, mesma classe de bug já documentada em `docs/PROGRESSO_MIGRACAO_USUARIO_GUID.md`.
>
> Última atualização: 2026-08-04. Itens fora dos módulos Matérias, Chat, Notificações, Configuração da escola, Auth/institucional e "Outros" refletem o estado confirmado em 2026-07-22 no relatório original — não foram re-auditados nesta rodada. **Chat foi re-auditado em 2026-07-29** e removido inteiro daqui: reações, gestão de Representante/Vice-Representante (com UI) e recibo de leitura já estavam implementados desde o commit `68d8cf3` (2026-07-23) — a documentação é que estava desatualizada, não o código. **Notificações também foi re-auditado em 2026-07-29**: o toast em tempo real já estava implementado (`NotificacaoToastListener`, montado no layout do dashboard); paginação/retenção do feed foi decidida pelo usuário (expurgo automático só de notificações já lidas, 30 dias) e implementada nesta mesma rodada — item removido daqui. **Dois gaps do Matérias também foram fechados em 2026-07-29**: edição de mídia de Conteúdo já publicado (texto/link/arquivo, por tipo) e link "Gerenciar grupo" no visualizador para tarefas compartilhadas (leva à tela `/tarefas/[tarefaGUID]` já existente, antes só alcançável navegando manualmente). **"Configuração da escola" e 2 dos 3 itens de "Auth/institucional" também caem em 2026-07-29**: e-mail/logo da escola e o rework de `saiba-mais` + reposicionamento de login/cadastro já tinham sido implementados no dia seguinte ao relatório original (commits `4a0970e` e `6b27275`, ambos 2026-07-23) — de novo, documentação desatualizada, não código faltando. **3 dos 4 itens de "Outros" também fecharam em 2026-07-29**: filtro de pesquisa e cards dinâmicos de Gestão de Dados já existiam (documentação desatualizada); o atalho visual "Minhas Matérias" na home do dashboard e a cobertura de CSS de acessibilidade em Toast/modais de grupo/bolha de chat/componentes de Matérias foram implementados nesta rodada (gestão de dados/calendário/projetos/tarefas já estavam cobertos de sessões anteriores, só não documentado).
>
> **Auditoria de código completa em 2026-08-04** (dois agentes, um focado na feature de IA recém-implementada, outro no resto do projeto): a Recomendação de Estudo por IA — listada abaixo desde sempre como "fora de escopo" — **na verdade já foi quase inteiramente implementada** (backend completo: `backend/ai/`, `assunto`/`materiaglobal`/`materialdidatico`/`questaobanco`, migrations fase1-3, rotas registradas; frontend: listbox de travamento manual de Assunto em `ProvaAgendadaForm.tsx`, tela `admin-plataforma` funcional pro banco de questões/fila de matéria global) — item removido daqui e substituído pelo gap real encontrado (tela de consumo pelo aluno). Dois bugs reais também foram encontrados e **corrigidos na hora** (não ficam pendentes): (1) `@google/genai` estava em `package.json` mas não instalado (`npm install` nunca rodou depois de adicionar a dependência — backend não compilava); (2) IDOR em anexos — `AnexoService.buscarAnexo`/`downloadAnexo` não validavam se o usuário tinha vínculo com a escola dona do anexo (qualquer usuário autenticado de qualquer escola lia/baixava anexo de outra escola sabendo o GUID) — corrigido em `backend/services/anexo.service.ts` reaproveitando `EscolaxUsuarioxFuncaoDAO`; (3) o fallback de proximidade temporal da Recomendação de Estudo (`provaagendadarecomendacao.service.ts`) buscava `Conteudo` de **todas** as turmas da matéria em vez de só as turmas da prova quando nenhuma delas usava categoria — corrigido pra escopar por `ConteudoTurma` das turmas da prova antes de aplicar a proximidade temporal. Também confirmado nesta rodada: a troca de capa/cor da turma pelo Representante (`PLANO_IMPLEMENTACAO_MATERIAS.md`, decisão #8) tem backend 100% pronto (`TurmaService.atualizarCapa`, rota, client de API) mas **nenhuma UI chama isso** — vira item novo abaixo, separado do gap já conhecido de "categoria do representante".
>
> **Segunda rodada de auditoria, mesmo dia (2026-08-04)** — 4 agentes em paralelo, um por área (módulos acadêmicos; chat/notificações/calendário/projetos; segurança/infra cross-cutting; frontend + pensamento de escopo de produto). **5 bugs reais encontrados e corrigidos na hora:** (1) `JWT_SECRET` tinha fallback hardcoded no código-fonte (`backend/utils/JwtService.ts`) — sem a env var setada em produção, qualquer um forjava token válido pra qualquer usuário; agora o servidor recusa iniciar sem `JWT_SECRET` definido, sem fallback nenhum; (2) CORS estava com `origin: "*"` (`backend/Server.ts`) — trocado por allowlist via `CORS_ORIGIN`/`FRONTEND_URL`; (3) login (`POST /api/auth/login`) e cadastro (`POST /api/usuario`) não tinham rate limit — sem proteção contra força bruta/spam; agora usam `authRateLimitMiddleware` (20 req/15min); (4) `MensagemService.marcarComoLida` (`backend/services/mensagem.service.ts`) era o único método do arquivo que não validava `isParticipante` — qualquer usuário autenticado conseguia se inserir na lista de "leitores" de qualquer conversa só sabendo o GUID; (5) o handler WebSocket `typing` (`backend/websocket/conversa.handler.ts`) tinha a mesma falta de checagem de participante que o `mark_as_read` original. Confirmado como seguro (não precisou de fix): sem SQL injection, sem segredo hardcoded além do JWT já citado, upload valida mimetype/tamanho no servidor (não só no client), sem caminho de escalação de privilégio pra `UsuarioIsPlataformaAdmin` via API, JWT expira corretamente. Itens que ficam pendentes (não foram corrigidos, só documentados) estão nas seções "Segurança / Infra" e "Produto" abaixo.
>
> **Fluxo "esqueci minha senha" implementado em 2026-08-04** — item que estava na seção Produto foi removido e fechado: `POST /api/redefinicao-senha/{solicitar,redefinir}` (token de 64 hex chars por e-mail, expira em 1h, sem enumeração de usuário — resposta de `/solicitar` é sempre a mesma mensagem genérica), páginas `frontend/app/{esqueci-senha,redefinir-senha}/page.tsx`, botão "Esqueci minha senha" do login (que só tinha um `alert()` de placeholder) agora aponta pra lá. Doc completa em `docs/routes/redefinicao-senha-api.md`. Migration `2026-08-04-redefinicao-senha.sql` ainda precisa ser aplicada no banco.
>
> **Mistério do "módulo em standby" (pág. 11 do board) resolvido pelo usuário em 2026-08-04**: era o banco de questões de vestibular — já implementado como `QuestaoBanco`/`QuestaoBancoAlternativa` dentro da Recomendação de Estudos por IA (§2.5/§3.5 de `docs/SPEC_RECOMENDACAO_ESTUDOS_IA.md`). Item removido de "Outros".
>
> **Logging estruturado implementado em 2026-08-04** (`winston`) — item removido da seção Produto. `backend/utils/logger.ts` (JSON em produção, colorizado em dev, nível via `LOG_LEVEL`) + `backend/middlewares/request-logger.middleware.ts` (log de toda requisição HTTP com `requestId`, devolvido também em `X-Request-Id`) + handler global de erro (`Server.ts`) e `uncaughtException`/`unhandledRejection` (que não existiam) migrados pro logger. **Deliberadamente não tocado:** os milhares de `console.log("🟣 Service.metodo()")` espalhados pelo resto do código — são rastro de execução, não observabilidade de produção; trocar todos é um projeto à parte, de valor marginal e alto risco de diff. **Bug real descoberto como efeito colateral:** ao testar o boot com o `JWT_SECRET` sem fallback (fix da rodada de segurança anterior), o servidor quebrava com "JWT_SECRET não está definido" mesmo com a variável presente no `.env` — causa raiz: TypeScript/tsx sempre hoisteiam `import` pro topo do arquivo compilado, então `dotenv.config()` em `app.ts` (mesmo escrito "antes" de `import Server`) na prática rodava **depois** de todo o grafo de módulos já ter sido carregado, incluindo `JwtService`. Isso sempre existiu — só nunca quebrou nada porque o fallback hardcoded mascarava o problema (ou seja: é bem possível que o servidor em produção **nunca tenha usado o `JWT_SECRET` real do ambiente**, e sim o fallback hardcoded, até este fix). Corrigido trocando `dev`/`start` em `package.json` pra pré-carregar `dotenv/config` via `-r` (`node -r dotenv/config` / `tsx watch -r dotenv/config`), que roda numa fase anterior ao carregamento do módulo de entrada — não sofre do hoisting. **Ação recomendada pro usuário: gerar um `JWT_SECRET` novo em produção** (o valor atual, se nunca foi usado de verdade, pode não ser o que vocês pensam que é — ou pior, pode ser literalmente o fallback antigo que estava no código-fonte público).

## Como ler

- Cada item tem uma tag entre parênteses:
  - **(bug/gap)** — falta de verdade, deveria ser feito.
  - **(fora de escopo)** — adiamento consciente do usuário, não é esquecimento. Não tratar como prioridade sem confirmar antes.
  - **(confirmar)** — incerteza real: pode já estar resolvido, mas não foi verificado no código.

---

## Matérias

- [ ] Nota de prova (`prova_nota`) — prova hoje é só leitura + "já vi", sem lançamento de nota **(fora de escopo)**
- [ ] Categoria "do representante" (aluno representante de turma criar categoria própria) — hook de permissão pronto no backend, sem UI **(fora de escopo, função futura confirmada)**

## Auth / institucional (Login, Cadastro, Saiba mais, Landing page)

- [ ] Revisão de copy/persuasão da landing page (confiança, argumentos de venda, FAQ) — decisão de conteúdo, não de layout **(confirmar com o usuário/negócio, não é trabalho de código — ver spec)**

## Consistência de código (achados da auditoria de 2026-08-04, não são bugs de comportamento)

- [ ] Feature de Recomendação de Estudo por IA (`assunto`/`materiaglobal`/`materialdidatico`/`questaobanco`) não usa Zod — controllers passam `req.body`/`req.query` direto pro service, validação só via setters de entidade + checagens inline no service. Funciona, mas é o estilo pré-migração; o resto do backend vem sendo migrado pra `backend/schemas/*.schema.ts` + `zodValidate` desde julho (`docs/PLANO_MIGRACAO_TAREFAS_ZOD_REACT_QUERY.md`) **(nice-to-have de consistência, não bug)**
- [ ] `AnexoService.validarPermissaoEscrita` (delete) — dono do arquivo ou Coordenação/Direção da escola podem excluir; **não** existe um caminho pra outros papéis (ex.: professor dono da turma) excluírem anexo de aluno — confirmar se isso é intencional **(confirmar)**
- [ ] Documentação de API (`docs/routes/*-api.md`) cobre todas as ~32 rotas pré-existentes, mas as 4 rotas novas da Recomendação de Estudo por IA (`assunto`, `materiaglobal`, `materialdidatico`, `questaobanco`) ainda não têm doc correspondente — lag da implementação recente, não problema sistêmico **(nice-to-have)**
- [ ] `ConteudoProgressoService` (`backend/services/conteudoprogresso.service.ts`) não valida que o `ConteudoGUID`/`ConteudoPaginadoArquivoGUID` pertence a uma turma em que o aluno está matriculado antes de gravar progresso — `MatriculaGUID` é sempre resolvido do próprio usuário autenticado, então não vaza dado de terceiro, mas um aluno consegue poluir as próprias estatísticas de progresso com conteúdo de turma que não é a dele **(bug/gap, baixa severidade)**

## Segurança / Infraestrutura (achados da auditoria de 2026-08-04 que ficam pendentes — os 5 já corrigidos estão na nota do topo)

- [ ] **`UsuarioIsPlataformaAdmin` não é o mesmo que "Direção de uma escola"** — criar uma escola vincula automaticamente o criador como Direção (FuncaoId=6) **daquela escola** (`EscolaService`, linha ~113), mas isso é um papel por-escola, não o flag global `UsuarioIsPlataformaAdmin` (gate de `/admin-plataforma`, banco de questões/fila de matéria global — não tem relação com nenhuma escola específica). Não existe nenhuma forma de setar esse flag global além de um `UPDATE` manual direto no banco. Não é uma falha de segurança (não existe caminho de escalação via API) **(confirmar com o usuário se é aceitável ficar manual — ver spec)**
- [ ] Zero testes automatizados em todo o projeto — `package.json` raiz tem `"test": "echo \"Error: no test specified\" && exit 1"`, frontend não tem setup de teste nenhum, nenhum arquivo `*.test.ts`/`*.spec.ts` existe **(gap grande, mas é uma decisão de escopo do TCC — confirmar se entra antes da entrega, ver spec)**

## Produto (sugestões de escopo — pensando no projeto como um todo, não bugs)

- [ ] **Sem forma de provisionar um banco novo do zero** — achado mais sério do que "falta seed": não existe schema.sql consolidado nem comando "rodar todas as migrations em ordem" (`backend/database/migrations/` tem ~50 arquivos `.sql`/`.ts` acumulados desde julho, sem script de execução em sequência); `QUICKSTART.md` manda só `CREATE DATABASE` e `npm run dev`, o que não cria nenhuma tabela. Um clone novo não tem como rodar o projeto contra um banco vazio, muito menos ter dados de demonstração **(sugestão, mas maior que o item original — ver plano em `docs/SPEC_PENDENCIAS_PRODUTO_2026-08-19.md`)**

## Outros

Sem itens no momento — o único que restava aqui ("módulo em standby") foi identificado, ver nota no topo do documento.
